<% page_title 'Your Dashboard' %>

<div class="tasks">
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= link_to task do %>
          <strong>[<i>#<%= task.id %></i>] <%= task %></strong>
        <% end %>
      </li>
    <% end %>
  </ul>
</div>

<!-- <div class="projects">
  <% if @projects.present? %>
  <ul>
    <% @projects.each do |project| %>
      <li><%= link_to project, project_path(project.accountable, project) %></li>
    <% end %>
  </ul>
  <% else %>
    <h3>You've not created or been added to any projects yet. <%= link_to 'Create one now...', new_project_path %></h3>
  <% end %>
</div>
 -->
