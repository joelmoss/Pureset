<% page_title @project %>

<% if @project.tasks.present? %>
  <ul>
    <% @project.tasks.each do |task| %>
      <li><%= link_to task %></li>
    <% end %>
  </ul>
<% else %>
  <h4>This project has no tasks. <%= link_to 'Create one now...', new_task_path %></h4>
<% end -%>
