<%= simple_form_for @task do |f| %>
  <div class="form-inputs">
    <%= f.input :summary %>
    <%= f.input :description, as: :text %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
